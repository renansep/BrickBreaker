var Level = function(vid)
{
	var matrix = new Array(15);
	for (var i=0; i<matrix.length; i++)
		matrix[i] = new Array(12);
			
	for (var i=0; i<matrix.length; i++)
	{
		for (var j=0; j<matrix[i].length; j++)
			matrix[i][j] = 1;
	}
		
	return matrix;
		
	if (vid == 1)
	{
		
	}
}